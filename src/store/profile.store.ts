// "use client";
// import { create } from "zustand";
// import { AuthState } from "@/interface/auth.types";

// const useAuthStore = create<AuthState>((set) => ({
//   user: null,
//   token: null,
//   setAuth: (user, token) => set({ user, token }),
//   signOut: () => set({ user: null, token: null }),
// }));

// export default useAuthStore;
